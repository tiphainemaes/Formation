
function tableauNotes (...mesNotes) {
  console.log(mesNotes);

}

tableauNotes(17, 14, 13, 11, 9, 3, 3, 11, 12, 13, 14);





















/*let readline = require("readline");
let read = readline.createInterface({
    input: process.stdin,
    output: process.stdout 
});

let tableau = [];
tableau.length = 9;
let i = 0


function demanderUneNote(){
    read.question("Entre une note : \n", function(note){
      tableau[i] = note;
      if (i != tableau.length - 1){
        i++;
        demanderUneNote();
      }
      else {
        console.log("Vous avez entrer les notes suivantes : \n" + tableau);
        read.close();
      }
    });
}

demanderUneNote();*/




